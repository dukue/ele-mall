// API基础URL
export const baseURL = 'http://localhost:3000/api/v1'

// 其他配置项
export const config = {
  // 每页显示数量
  pageSize: 10,
  // 图片上传大小限制（单位：MB）
  uploadLimit: 2,
  // 支持的图片格式
  allowedImageTypes: ['image/jpeg', 'image/png', 'image/gif']
} 